import { MessageConversation } from './message-conversation';

describe('MessageConversation', () => {
  it('should create an instance', () => {
    expect(new MessageConversation()).toBeTruthy();
  });
});
